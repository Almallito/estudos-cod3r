function divisao(dividendo, divisor) {
    return console.log(
        `
        Divisão: ${dividendo / divisor}
        Resto: ${dividendo % divisor}
        `
    )
}

divisao(10, 2)
divisao(10, 3)